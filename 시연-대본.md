# 시연 대본 — 엘리베이터 CAD → 파라메트릭 DB (한 방 데모)

> 실행: Git Bash에서 `bash run_demo.sh` (Windows PATH의 bash가 WSL이면 `"C:\Program Files\Git\bin\bash.exe" run_demo.sh`).
> 이 데모는 **IFC 경로**를 end-to-end로 돕니다. DXF·비정형(VLM/YOLO/LLM)은 인터페이스까지 설계한 후속 과업입니다.

## 한 줄 가치
임의의 엘리베이터 CAD(IFC)를 **부품·파라미터·의존 그래프**로 적재하고, **치수 하나를 바꾸면 전체가 재계산(전파)되고 3D가 재생성**됩니다. "그림"이 아니라 "레시피(파라미터+제약그래프+지오참조)"를 저장하는 게 핵심입니다.

## 스테이지별 말할 거리

**STAGE 1–2 · 추출**
합성 IFC를 만들고 파라메트릭 DB로 적재합니다. 미터 단위 도면을 **mm로 정규화**하고, 표준 사전으로 속성명을 canonical(car_width 등)에 정렬합니다. 여기서 보여줄 것: **커버리지 90%**, 표준 미매핑(FireRating)은 버리지 않고 **검수 큐**로, 그리고 의도적으로 심은 **EN81 위반 1건**(door_width 1200 > car_width 1100) 검출.

**STAGE 3–4 · 의존 그래프 자동 추론**
같은 시리즈 도면 5개에서 파라미터 간 관계를 회귀로 발굴합니다. 핵심 멘트:
> "여러 도면에서 `door_height = car_height − 100` 관계를 **데이터만으로 R²=1.0**로 되찾습니다. 방향이 모호한 관계(상관만으로는 인과 방향을 못 정하는 경우)는 **자동 기록하지 않고 사람 확인으로** 넘깁니다 — 잘못된 인과 방향을 기록하면 모델이 오염되니까요. 비선형(면적=폭×깊이)은 선형 회귀로 안 잡혀 표준 시드가 담당합니다."

**STAGE 5 · 전파 (핵심 장면)**
> "car_width만 1100→1300으로 바꿨는데, **shaft_width가 표준식대로 1600→1800** 따라가고, 그 파생인 **shaft_area가 2.88M→3.24M**(2단 체인)로 재계산됩니다. 동시에 **EN81 위반이 1건→0건**으로 해소됩니다. 측정값은 불변으로 두고, 솔버 결과만 별도 스냅샷(solved_parameter)에 분리 저장합니다. 표현식은 eval이 아니라 AST 화이트리스트로 안전하게 평가합니다."

**STAGE 6–7 · 3D 재생성 + 렌더**
> "전파된 파라미터로 3D 형상(카·승강로·도어)을 재생성하고, 생성된 형상의 bbox를 **다시 측정해 입력 파라미터와 일치하는지 역검증(7/7 PASS)**합니다 — car 폭 1300, shaft 폭 1800이 형상에서 그대로 나옵니다. 그 형상을 렌더해 **승강로 안에 카가 제대로 들어가는지 눈으로** 확인합니다. 생성물은 표준 IFC라 BIMvision/Revit에서도 열립니다."

**STAGE 8 · 측정**
> "추출 품질 하니스입니다. **추출된 원본 도면**의 커버리지·EN81·검수 큐를 집계합니다. 여기서 EN81 위반이 1로 나오는데, STAGE 5에서 0이 된 것과 다릅니다 — **측정은 원본 추출 품질을, 전파는 변경 시나리오 결과를 보는 서로 다른 단계**이기 때문입니다(원본 도면엔 위반이 실제로 1건 존재)."

## 정직한 한계 (반드시 언급)
- **정확도 vs 커버리지**: 측정 커버리지는 100%에 가깝지만 이는 **합성/표준 픽스처** 기준입니다. 진짜 추출 정확도는 **실제 제조사 도면 + 정답 치수표**가 있어야 측정됩니다. 실데이터가 오면 `python measure.py <새db> --ifc <파일.ifc>`로 **추출+측정 한 방**(새 db라 도면 1개로 깔끔)에 측정하도록 설계해 뒀습니다 — 한계를 알고 만든 것입니다.
- **DXF·비정형**: DXF 경로($INSUNITS 단위 + DIMENSION 자동연관)는 README·인터페이스까지 설계했으나 이 데모는 IFC만 돕니다. 스캔/레거시 도면용 비전(VLM/YOLO)과 시맨틱 매핑(LLM)은 **동일 인터페이스(시임)까지 두고 모델 연결만 남긴** 후속 과업입니다.

## 한 줄 마무리
> "구조화 도면은 **추출→전파→3D 재생성**까지 end-to-end로 돕니다. 비정형용 비전·LLM은 설계까지 끝내고 모델만 붙이면 됩니다."
