# Prisma + Next.js need hoisting of certain binaries; keep node-linker default.
# Ensure workspace packages resolve their own peer deps predictably.
auto-install-peers=true
strict-peer-dependencies=false
