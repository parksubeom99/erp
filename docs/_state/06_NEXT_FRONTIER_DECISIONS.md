# STEP 5 — 다음 프론티어 결정 패키지

> **이건 구현이 아니라 "결정 패키지"다.** 각 노드에 옵션 + 권장 + 근거. **결정은 회장님.** 착수는 회장님 지시 후.

## 결정 노드 A — 다음 실구현 대상

| 옵션 | 내용 | 장점 | 근거 |
|---|---|---|---|
| **A. STEP 6 (실행 고리)** ⭐권장 | DB-backed DataProvider로 `edim run` 실데이터 실행 → compile→approve→**run** 고리 닫기 | 스키마 무변경, base=`ef34c37` 명확, AI 트랙을 "동작"으로 완성, dry-run 실데이터·NOVA토큰 실동작도 동시 해결 | lmd §5·§7, 04_GAP B1 |
| B. L3 CPQ MVP | 로드맵 Phase2 제품선정 | 업무가치 직접, 흐름지도 BOM/CPQ 축 | 03_RECON §4, ROADMAP |
| C. 병합·정비 먼저 | 6 PR 병합 + TS CI 배선 + DB 게이트 green | 스택을 main에 안착시켜 이후 작업 토대 확보 | 04_GAP D2·B2·V2 |

> 세 옵션은 배타적이지 않음 — C(정비)를 먼저 얇게 한 뒤 A(STEP6)가 가장 안전한 순서로 보임.

## 결정 노드 B — 병합 전략 (D2)

스택 전체가 main과 단절(skeleton PR 부재). 옵션:
- **B1. skeleton→main PR 신설 후 bottom-up 병합** (#14→#15→#16→#17→#18, 그리고 #13·#19). 정공법.
- **B2. base 재타겟** — 각 PR base를 main으로 재지정(스택 평탄화). 리뷰는 쉬우나 이력 복잡.
- 병합 시 **0003 마이그레이션 충돌**(D3) 반드시 처리(나중 병합본 `0004_*`).

## 결정 노드 C — 모델 소싱 (확정본 재확인)

파트① §5 3안 중 **B 하이브리드 = 재가 완료**(Claude Sonnet 런치, env-swappable, `compile.ts` 반영). STEP 6에서 승인본 실행은 LLM 무관(결정론) — **추가 API 비용 없음.** 재론 불필요, 확인만.

## 결정 노드 D — 외부 대기 (비차단)

- **NOVA 3건**(FES·Cos2·PreC) Ian 회신 — 현재 "경고"로 안전 격리, 진행 무지장(D6).
- **auth 프로덕션 프로바이더**(D5) — 프로덕션 착수 전까지 결정 유예 가능.

## 결정 노드 E — 정비 항목 (저위험, 승인 시 CC 자율 가능)

- TS CI 배선(B2) · 로컬 remote URL 갱신(D7) · `claude/*` 잔재 브랜치 정리 · 85-MD 코퍼스 커밋(D4).

---

### 권장 다음 세션 진입 (엘 견해, 결정 아님)

1. **오늘 검토**: 00_STEP0 대조표(EDIM vs edlm) 확인 → D1 방향 지시.
2. **얇은 정비**(옵션 C 일부): DB 게이트 green 확인(V2) + TS CI 배선(B2).
3. **STEP 6 착수**(옵션 A): base=`ef34c37`, 엘이 STEP 6 ccmd 저작 → CC 투입.
4. **2번 사진(4516) 설명** 반영 → 흐름지도·정합 맵 갱신.
